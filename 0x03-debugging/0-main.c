#include "main.h"
/**
* main - tests function that prints if integer is positive or negative
* this is so messed up
* since i have to add 2 more lines to make my code work
* Return: 0
*/
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}
