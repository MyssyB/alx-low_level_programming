#include <stdio.h>
/**
* main - causes an infinite loop
* now tis has to be
* 24 lines long to work
* two more lines
* one more line
* Return: 0
*/
int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");
	i = 0;
	/**
	 * while (i < 10)
	 * {
	 *	putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
