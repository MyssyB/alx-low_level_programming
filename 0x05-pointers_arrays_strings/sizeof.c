#include <stdio.h>
#include <stdlib.h>

int mani(void)
{
	int n;
	printf("size of integer: %lu bytes\n", sizeof(int));
	printf("size of char: %lu bytes\n", sizeof(char));
	printf("size of double: %lu bytes\n", sizeof(double));
	return (0);
}
