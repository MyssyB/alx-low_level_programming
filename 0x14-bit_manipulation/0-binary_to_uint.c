#include <stdio.h>
#include <stdlib.h>
/**
 * unsigned int binary_to_uint - converts a bin to an unsigned int.
 * @b: is a pointer to a string
 *
 * Return: converted number or 0 if there is one or more char or b is NULL.
 */
unsigned int binary_to_uint(const char *b)
{

